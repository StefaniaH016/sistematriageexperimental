name: CD

on:
  push:
    branches: [main]

permissions:
  contents: read
  packages: write

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Build backend Docker image
        run: |
          OWNER=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          docker build -t ghcr.io/$OWNER/triage-backend:${{ github.sha }} .

      - name: Push backend Docker image
        run: |
          OWNER=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          docker push ghcr.io/$OWNER/triage-backend:${{ github.sha }}

      - name: Build frontend Docker image
        run: |
          OWNER=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          docker build -t ghcr.io/$OWNER/triage-frontend:${{ github.sha }} ./frontend

      - name: Push frontend Docker image
        run: |
          OWNER=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          docker push ghcr.io/$OWNER/triage-frontend:${{ github.sha }}

      - name: Tag latest images
        run: |
          OWNER=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          docker tag ghcr.io/$OWNER/triage-backend:${{ github.sha }} ghcr.io/$OWNER/triage-backend:latest
          docker push ghcr.io/$OWNER/triage-backend:latest
          docker tag ghcr.io/$OWNER/triage-frontend:${{ github.sha }} ghcr.io/$OWNER/triage-frontend:latest
          docker push ghcr.io/$OWNER/triage-frontend:latest
